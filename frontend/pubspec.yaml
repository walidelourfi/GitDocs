name: gitdocs
description: GitDocs — README Generator
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  flutter_markdown: ^0.6.18
  google_fonts: ^6.1.0
  url_launcher: ^6.2.5
  go_router: ^14.0.0
  file_picker: ^8.0.0
  markdown: ^7.2.0

flutter:
  uses-material-design: true
